" Color scheme
set background=dark
colorscheme solarized

set tags^=./.git/tags;

" make test commands execute using dispatch.vim
let test#strategy = "dispatch"

let g:ale_linters = {
\   'sass': ['scss-lint'],
\   'ruby': ['rubocop'],
\}
